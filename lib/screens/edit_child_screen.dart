// Placeholder for editing child details (future enhancement)
import 'package:flutter/material.dart';

class EditChildScreen extends StatelessWidget {
  const EditChildScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Edit Child')),
      body: const Center(child: Text('Edit child form coming soon')),
    );
  }
}
